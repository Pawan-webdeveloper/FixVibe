{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"],
    // axe-core runs inside the page, so its rule types describe browser
    // globals this service never touches itself.
    "paths": {
      "@scanlyfix/checks": ["../../packages/checks/src/index.ts"]
    }
  },
  "include": ["src/**/*.ts"]
}
